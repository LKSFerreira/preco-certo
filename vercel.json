{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/cosmos/:path*",
      "destination": "/api/cosmos/:path*"
    }
  ]
}
