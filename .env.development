# =============================================================================
# Variáveis de Ambiente - Desenvolvimento
# =============================================================================

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=preco_certo

# Conexão com o banco (usado pelo app)
DATABASE_URL=postgresql://postgres:postgres@database:5432/preco_certo

# VITE_OPENROUTER_TOKEN=sua_chave_aqui (Configure no .env.local)
# VITE_GOOGLE_TOKEN=sua_chave_aqui (Configure no .env.local)

# Supabase (para quando integrarmos)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
